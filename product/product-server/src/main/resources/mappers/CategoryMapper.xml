<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.order.mappers.CategoryMapper">
    <select id="findAllCategory" resultType="productCategory">
        select * from product_category;
    </select>
</mapper>